version: '3.8'

services:
  dfreeze-frontend:
    image: ${DOCKER_USERNAME:-your_username}/dfreeze-frontend:latest
    container_name: dfreeze-frontend
    restart: unless-stopped
    ports:
      - "4321:4321"
    env_file:
      - .env
